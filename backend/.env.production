# 生产环境配置 - Vercel 部署
# 在 Vercel 控制面板的 Environment Variables 中设置这些变量

NODE_ENV=production
PORT=3000

# JWT 配置 - 生成强随机密钥
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secure_jwt_secret_key_here_min_32_chars

# JWT 过期时间
JWT_EXPIRE=7d

# CORS 配置 - 设置为你的前端部署地址
# 示例: https://expense-tracker-frontend.vercel.app
CORS_ORIGIN=https://your-frontend-domain.vercel.app

# 数据库配置
# 选项 1: SQLite Cloud
# DATABASE_URL=https://api.sqlitecloud.io/your-api-key

# 选项 2: PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/database

# 选项 3: MySQL
# DATABASE_URL=mysql://user:password@host:port/database

# 选项 4: 本地 SQLite (Vercel 不推荐 - 文件会重置)
# DATABASE_URL=sqlite:./data/expense_tracker.db

# 选项 5: Vercel PostgreSQL
# DATABASE_URL=postgres://default:password@host:port/verceldb

# 日志级别
LOG_LEVEL=error

# 应用模式
APP_MODE=api
